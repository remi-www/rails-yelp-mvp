<div class="container">
  <h2>Add a review</h2>
  <%= simple_form_for [ @restaurant, @review ] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, collection: 0..5 %>
    <%= f.submit class: "btn btn-light" %>
  <% end %>
</div>
